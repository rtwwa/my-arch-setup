✨ Конфиг для archlinux

🚀Для запуска пропишите в терминал:

```
git clone https://github.com/you/my-arch-setup.git
cd my-arch-setup
chmod +x install.sh
./install.sh

```

Не забудьте настроить gh cli

```
gh auth login
```
